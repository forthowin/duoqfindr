= render '/shared/content_title', title: 'Forgot Password?'
%p We will send you an email with a link that you can use to reset your password.

= bootstrap_form_tag url: forgot_passwords_path do |f|
  .form-group
    .col-sm-6
      = f.text_field :email
      = f.primary 'Send Email'